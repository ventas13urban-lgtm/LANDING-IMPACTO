export interface Project {
  id: string;
  title: string;
  category: string;
  image: string;
  description: string;
  outcome: string;
}

export interface MenuItem {
  label: string;
  href: string;
}